CustomWijmoControl
--------------------------------------------------------------------
Shows how to create custom controls by inheriting from the wijmo.Control class.

The sample defines a custom control called 'MyControl' and creates
two instances of it on a page.