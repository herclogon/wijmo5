(function (helper, props) {
    'use strict';

    // init Wijmo controls
    var linearGauge = helper.createLinearGauge('#gsLinearGauge', props),
        radialGauge = helper.createRadialGauge('#gsRadialGauge', props),
        valueInput = helper.createValueInput('#gsValue', props);

})(Helper, props);